//This is placeholder of piece type for each player. 
//Currently, it has four types for four players.
// This can be extended if more players are added.
//This is used while initializing players.
char* PieceIdentifier[] = {
    "X","O", "@", "#"
};